services:
  frontend:
    image: node:22

    working_dir: /app

    volumes:
      - ./frontend:/app

    ports:
      - "5173:5173"

    environment:
      - CHOKIDAR_USEPOLLING:"true"

    command: npm run dev -- --host 0.0.0.0

    stdin_open: true
    tty: true